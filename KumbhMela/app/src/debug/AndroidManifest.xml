<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shubh.log">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".map"
            android:label="@string/title_activity_map" />
        <activity
            android:name=".maptwo"
            android:label="@string/title_activity_maptwo" />
        <activity
            android:name=".mapthree"
            android:label="@string/title_activity_mapthree" />
        <activity
            android:name=".mapfour"
            android:label="@string/title_activity_mapfour" />
        <activity android:name=".travel" />
        <activity android:name=".flight" />
        <activity android:name=".railways" />
        <activity android:name=".roadways" />
        <activity android:name=".tourism" />
        <activity android:name=".spota" />
        <activity android:name=".spotb" />
        <activity android:name=".spotc" />
        <activity android:name=".spotd" />
        <activity android:name=".fback" />
        <activity android:name=".usercomment" />
        <activity android:name=".notification" />
        <activity android:name=".notifications" />
        <activity android:name=".comments" />
        <activity android:name=".com"></activity>
    </application>

</manifest>